spring:
  application:
    name: demo

# ---------------------------------------------------------------------------
# OAuth 2.0 / OIDC configuration
#
# Everything below is intentionally commented out so the starter can boot
# without a provider. Uncomment and complete it while implementing the
# assignment with the mock OIDC provider on http://localhost:9000 (issuer: /default).
# ---------------------------------------------------------------------------
#  security:
#    oauth2:
#      # --- OIDC login (browser session, identity for /api/me) --------------
#      client:
#        provider:
#          mock-oidc:
#            # Discovery document: <issuer-uri>/.well-known/openid-configuration
#            issuer-uri: http://localhost:9000/default
#        registration:
#          mock-oidc:
#            provider: mock-oidc
#            client-id: user-api
#            client-secret: user-api-secret
#            authorization-grant-type: authorization_code
#            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
#            scope:
#              - openid
#              - profile
#              - email
#
#      # --- Resource server (JWT bearer tokens for /api/profile) ------------
#      resourceserver:
#        jwt:
#          # TODO: Candidate to implement... configure token validation using
#          # issuer discovery and/or JWKS from the same mock provider.
#          issuer-uri: http://localhost:9000/default
#          # jwk-set-uri: http://localhost:9000/default/jwks

server:
  port: 8080

logging:
  level:
    org.springframework.security: INFO
    # Switch to TRACE while debugging the OAuth2 / OIDC flows:
    # org.springframework.security: TRACE
